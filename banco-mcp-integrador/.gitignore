# Configuracao com chaves/API key — NUNCA versionar
config.json

# Dados financeiros baixados/gerados
dados_banco.json

# Extratos bancarios (dados financeiros) — NUNCA versionar.
# A pasta continua no repo (via .gitkeep), mas os arquivos de extrato nao.
extratos/*.csv
extratos/*.ofx

# Python
__pycache__/
*.pyc
.venv/
venv/
